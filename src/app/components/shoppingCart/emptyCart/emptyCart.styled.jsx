import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const Conatiner = styled(Box)({
  position: 'relative',
  display: 'block',
  height: '100%',
  marginTop: '16px',
});
